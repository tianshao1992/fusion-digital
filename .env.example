# 可选：不配置时，/search 和确定性知识检索仍可使用；/api/ask 自动回退到检索结果。
# 只把真实密钥写入本机 .env.local，绝对不要提交到 Git。
OPENAI_API_KEY=

# 可选：覆盖服务端问答模型。为空时使用代码中的默认模型。
OPENAI_MODEL=

# 可选：`npm run start` 的监听端口。未设置时为 3000。
# PORT=3000
